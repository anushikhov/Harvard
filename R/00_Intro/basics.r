a <- 1
b <- 2
c <- -1

ls()

(-b + sqrt(b^2 - 4*a*c)) / (2*a)

log(8)

# Typing the name of the function, without parenthesis
ls

log(a)

# Nested functions are evaluated from the inside out
log(exp(1))

# To learn about functions
help("log")

# or

?log

# Reminder of the needed arguments
args(log)

# To change the default value
log(8, base=2)

log(x=8, base=2)

log(8, 2)

help("+")

?"+"

# To see all the available datasets
data()

# Will see CO2 measurements from a particular measurement stage
CO2

pi

Inf

